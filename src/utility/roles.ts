const Roles = {
  public: 'public',
  manufacturer: 'manufacturer',
  distributor: 'distributor',
  pharmacy: 'pharmacy',
};

export interface RoleTypes {
  manufacturer: string;
  distributor: string;
  pharmacy: string;
}

export default Roles;
