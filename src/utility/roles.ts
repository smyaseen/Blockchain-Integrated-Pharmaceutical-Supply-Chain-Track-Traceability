const Roles = {
  manufacturer: 'manufacturer',
  distributor: 'distributor',
  pharmacy: 'pharmacy',
  customer: 'customer',
};

export interface RoleTypes {
  manufacturer: string;
  distributor: string;
  pharmacy: string;
  customer: string;
}

export default Roles;
